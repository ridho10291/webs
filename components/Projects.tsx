"use client";import { motion } from "framer-motion";import { ExternalLink, Github } from "lucide-react";import { projects } from "@/data/projects";import { SectionHeading } from "./SectionHeading";export function Projects() {return (<section id="proyek" className="relative py-24 sm:py-32 px-4"><div className="mx-auto max-w-6xl"><motion.div className="text-center" initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, margin: "-100px" }} transition={{ duration: 0.7 }}><SectionHeading badge="Project Pilihan" title="Beberapa sistem yang sudah saya bangun & deploy" subtitle="Semua live, scalable, dan di-hosting gratis di Vercel + Neon. Klik untuk lihat kode atau demo." /></motion.div><motion.div className="mt-16 grid gap-6 sm:grid-cols-2 lg:grid-cols-3" initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} viewport={{ once: true, margin: "-100px" }} transition={{ duration: 0.7, delay: 0.1 }}>{projects.map((project, i) => (<motion.article key={project.title} className="group relative overflow-hidden rounded-2xl border border-border/50 bg-bg-elevated/50 p-6 transition-all duration-500 hover:border-primary/50 hover:shadow-[0_25px_50px_rgba(0,0,0,0.4)] hover:shadow-primary/10 hover:-translate-y-2" initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.1 + i * 0.08, duration: 0.5 }}>{project.featured && (<motion.div className="absolute top-4 right-4 rounded-full bg-gradient-to-r from-primary to-accent px-3 py-1 text-[10px] font-bold tracking-widest text-white uppercase" initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ delay: 0.3 + i * 0.08, type: "spring", stiffness: 260 }}>Featured</motion.div>)}<motion.div className="flex h-14 w-14 items-center justify-center rounded-2xl text-3xl" style={{background: "linear-gradient(to bottom right, var(--primary) / 10%, var(--accent) / 10%)"}} whileHover={{ scale: 1.1, rotate: [0, -3, 3, 0] }}>{project.emoji}</motion.div><h3 className="mt-5 text-xl font-bold text-text group-hover:text-primary transition-colors">{project.title}</h3><p className="mt-3 text-text-muted leading-relaxed line-clamp-3">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tech.map((tech) => (<span key={tech} className="rounded-lg border border-border/50 bg-bg/50 px-3 py-1 text-xs font-medium text-text-muted transition-all hover:border-primary/50 hover:text-text">{tech}</span>))}</div><div className="mt-6 flex items-center gap-3 border-t border-border/50 pt-4">{project.source && (<a href={project.source} target="_blank" rel="noreferrer" className="flex-1 flex items-center justify-center gap-2 rounded-xl border border-border/50 bg-bg-elevated/50 px-4 py-2.5 text-sm font-medium text-text-muted transition-all hover:border-primary/50 hover:bg-primary/10 hover:text-text"><Github size={16} />Kode</a>)}{project.demo && (<a href={project.demo} target="_blank" rel="noreferrer" className="flex-1 flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-primary to-accent px-4 py-2.5 text-sm font-bold text-bg shadow-lg shadow-primary/30 transition-all hover:shadow-primary/50 hover:-translate-y-0.5"><ExternalLink size={16} />Demo</a>)}</div></motion.article>))}</motion.div><motion.div className="mt-16 text-center" initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} viewport={{ once: true, margin: "-100px" }} transition={{ duration: 0.7, delay: 0.5 }}><p className="text-text-muted mb-4">Mau lihat lebih banyak? Atau punya ide project sendiri?</p><a href="#kontak" className="inline-flex items-center gap-2 rounded-xl bg-gradient-to-r from-primary to-accent px-6 py-3 font-semibold text-bg shadow-lg shadow-primary/30 transition-all hover:shadow-primary/50 hover:-translate-y-0.5">Mulai Project Bareng<ExternalLink size={18} /></a></motion.div></div></section>);}